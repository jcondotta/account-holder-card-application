micronaut:
  server:
    port: 8190

  jms:
    sqs:
      enabled: true

aws:
  access-key-id: 'test'
  secret-key: 'test'
  region: 'us-east-1'

  sns:
    endpoint: ${AWS_SNS_ENDPOINT}
  sqs:
    endpoint: ${AWS_SQS_ENDPOINT}